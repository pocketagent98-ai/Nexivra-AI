[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nexivra-ai"
version = "0.2.0"
description = "Nexivra AI — evidence-first autonomous research and agent platform (proprietary, source-available)"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Proprietary — All rights reserved. See LICENSE." }
authors = [{ name = "Nexivra AI" }]
dependencies = [
    "cryptography>=42.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[tool.setuptools.packages.find]
include = ["nexivra*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-q"
